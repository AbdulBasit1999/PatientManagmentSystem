import {Component, React} from "react";

export default class Home extends Component{
    render(){
        return(
            <div>
                <h1>hello there</h1>
            </div>
        );
    }
}